name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - name: Build binaries
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          mkdir -p dist
          # Stamp the pushed tag (e.g. v0.1.0) into the binary so `antics version` reports it.
          LDFLAGS="-X main.version=${VERSION}"
          GOOS=darwin GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/antics-macos-arm64 ./cmd/
          GOOS=darwin GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/antics-macos-intel ./cmd/
          GOOS=linux  GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/antics-linux-amd64 ./cmd/
      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
